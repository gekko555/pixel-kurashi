レビューありがとうございます。計画は確定しました。

# 最終実装計画

## 1. DDL修正（category_master作成→money_recordのcategory_id・外部キー・インデックス）

**ファイル**: `sql/ddl/category_master.sql`（新規作成）
```sql
CREATE TABLE category_master (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL COMMENT 'カテゴリ名',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '作成日',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新日'
) COMMENT ='カテゴリマスタ';
```

**ファイル**: `sql/ddl/money_record.sql`
```sql
CREATE TABLE money_record (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    record_date DATE NOT NULL COMMENT '記録日',
    amount INT NOT NULL COMMENT '金額(収入:+, 支出:-)',
    category_id BIGINT NOT NULL COMMENT 'カテゴリID',
    memo VARCHAR(255) COMMENT 'メモ',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '作成日',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新日',
    INDEX idx_money_record_record_date(record_date),
    INDEX idx_money_record_category_id(category_id),
    CONSTRAINT fk_money_record_category
        FOREIGN KEY (category_id) REFERENCES category_master(id)
) COMMENT ='家計簿記録';
```

**注意**: `category_master`を先に作成してから`money_record`の外部キーを作成してください。

## 2. 日付検索を範囲条件に変更（startDate/endDate・インデックス活用）

**ファイル**: [MoneyRecordMapper.xml](cci:7://file:///c:/Users/hide/Desktop/pixel-kurashi/backend/src/main/resources/mybatis/mapper/MoneyRecordMapper.xml:0:0-0:0)
```sql
WHERE record_date >= #{startDate} AND record_date < #{endDate}
```

**ファイル**: [MoneyRecordRepository.java](cci:7://file:///c:/Users/hide/Desktop/pixel-kurashi/backend/src/main/java/com/example/pixelkurashi/repository/MoneyRecordRepository.java:0:0-0:0)
```java
List<MoneyRecordResponse> findByMonth(
    @Param("startDate") LocalDate startDate,
    @Param("endDate") LocalDate endDate
);
```

**ファイル**: [MoneyRecordService.java](cci:7://file:///c:/Users/hide/Desktop/pixel-kurashi/backend/src/main/java/com/example/pixelkurashi/service/MoneyRecordService.java:0:0-0:0)
```java
public List<MoneyRecordResponse> getRecordsByMonth(int year, int month) {
    LocalDate startDate = LocalDate.of(year, month, 1);
    LocalDate endDate = startDate.plusMonths(1);
    return repository.findByMonth(startDate, endDate);
}
```

**注意**: Serviceテストも更新が必要です。

## 3. CategoryController/Serviceをコンストラクタ注入に変更

**ファイル**: `CategoryController.java`, [CategoryMasterService.java](cci:7://file:///c:/Users/hide/Desktop/pixel-kurashi/backend/src/main/java/com/example/pixelkurashi/service/CategoryMasterService.java:0:0-0:0)

フィールドインジェクションからコンストラクタ注入に変更。

## 4. Repository戻り値整理（MoneyRecordWithCategory作成）

**新規ファイル**: `MoneyRecordWithCategory.java`
```java
public class MoneyRecordWithCategory {
    private Long id;
    private LocalDate recordDate;
    private Integer amount;
    private Long categoryId;
    private String categoryName;
    private String memo;
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
    // コンストラクタ・getter・setter
}
```

**ファイル**: [MoneyRecordRepository.java](cci:7://file:///c:/Users/hide/Desktop/pixel-kurashi/backend/src/main/java/com/example/pixelkurashi/repository/MoneyRecordRepository.java:0:0-0:0)
- 戻り値を`List<MoneyRecordWithCategory>`に変更

**ファイル**: [MoneyRecordService.java](cci:7://file:///c:/Users/hide/Desktop/pixel-kurashi/backend/src/main/java/com/example/pixelkurashi/service/MoneyRecordService.java:0:0-0:0)
- `MoneyRecordWithCategory`を[MoneyRecordResponse](cci:2://file:///c:/Users/hide/Desktop/pixel-kurashi/frontend/src/types/moneyRecord.ts:0:0-9:1)に変換

## 5. fetchをAPI関数に分離（その後でOK）

**新規ファイル**: `frontend/src/api/moneyRecords.ts`

---

どのタスクから実装しますか？